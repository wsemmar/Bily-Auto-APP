import React from "react";

const Filter = () => {
  return (
    <div className="filter">
      <div>
        <span>Filters: </span>
        <select>
          <option value="">All Products</option>
        </select>
      </div>
    </div>
  );
};

export default Filter;
